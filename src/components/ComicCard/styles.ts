import { StyleSheet } from "react-native";

import { palette } from "@/themes";

export const styles = StyleSheet.create({
  container: {
    width: 150,
    height: 280,
    marginRight: 8,
  },
  title: {
    color: palette.black,
    fontSize: 12,
    lineHeight: 16.8,
  },
  comicImage: {
    height: 225,
  },
});
